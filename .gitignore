cookup.db
uploads/
__pycache__/
*.pyc
.env

# Local tooling, ML weights, scratch outputs — not for GitHub or Render
misc/
.pytest_cache/

# Exceeds GitHub's 100 MB blob limit — keep locally or use Git LFS
dataset/trap/synths.zip
